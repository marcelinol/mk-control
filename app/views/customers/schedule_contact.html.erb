<%= form_for @customer, url: update_customer_path(@customer) do |f| %>
  <p>Próximo contato para:
  <p><%= f.date_field :next_contact_date, value: 2.days.from_now.to_date %>
  <p>Alguma observação?
  <p><%= f.text_area :observations %>
  <p><%= f.submit "Salvar" %>
<% end %>
